from pathlib import Path

BASE = Path(__file__).parent
SUBJECTS = BASE / "resources"
REP = BASE / "rep"
SFL = BASE / "sflkit_events"
LOCALIZATION = REP / "localization"
REF_BENCHMARK = REP / "refactory_benchmark"
